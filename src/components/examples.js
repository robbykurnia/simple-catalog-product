import Clock from './clock';
import Counter from './counter';

const Examples = () => (
    <div style={{ marginBottom: 10 }}>
      <Clock />
      <Counter />
    </div>
  );

export default Examples;
